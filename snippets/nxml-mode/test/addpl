d
addplugin(${1:var});
d
    set(plugin[$1].align,);
d
    set(plugin[$1].alpha,);
d
    set(plugin[$1].crop,);
d
    set(plugin[$1].children,);
d
    set(plugin[$1].edge,);
d
    set(plugin[$1].handcursor,);
d
    set(plugin[$1].height,);
d
    set(plugin[$1].keep,);
d
    set(plugin[$1].onclick,);
d
    set(plugin[$1].onover,);
d
    set(plugin[$1].onout,);
d
    set(plugin[$1].url,%HTMLPATH%/files/images/);
d
    set(plugin[$1].visible,);
d
    set(plugin[$1].width,);
d
    set(plugin[$1].x,);
d
    set(plugin[$1].y,);
d
    set(plugin[$1].zorder,);
d
$0